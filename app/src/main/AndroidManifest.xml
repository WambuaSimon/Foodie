<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.foodie">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.Activity_ViewFoods"></activity>
        <activity android:name=".activities.Activity_CustomerVerification" />
        <activity android:name=".activities.Acitivity_CustomerLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".activities.Activity_Orders" />
        <activity android:name=".activities.Activity_SetFood" />
        <activity android:name=".activities.Activity_Verify_Phone" />
        <activity android:name=".activities.Activity_Admin_Login">

        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>